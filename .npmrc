# Fail fast when npm runs under a Node outside the engines range below —
# a mismatched local Node (e.g. an EOL odd release) silently rewrites
# package-lock.json in ways that break `npm ci` on Linux.
engine-strict=true
