# A USB printer (interface class 07/subclass 01) appearing — at boot or
# hot-plugged — kicks off the FoodDesk auto-queue service.
ACTION=="add", SUBSYSTEM=="usb", ENV{ID_USB_INTERFACES}=="*:0701*", TAG+="systemd", ENV{SYSTEMD_WANTS}+="fooddesk-printer-setup.service"
